const navLinks = {
    defaultLinks: [
        {
			name: "Create a Team",
			to: "/createteam",
		},
        {
            name: "About",
            to: "/about"
        }
    ],
};

export default navLinks;