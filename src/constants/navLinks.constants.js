const navLinks = {
    defaultLinks: [
        {
			name: "Join a Team",
			to: "/teams",
		},
        {
            name: "About",
            to: "/about"
        }
    ],
};

export default navLinks;