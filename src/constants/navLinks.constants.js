const navLinks = {
    defaultLinks: [
        {
			name: "Create a Team",
			to: "/createteam",
		},
        {
            name: "About",
            to: "/about"
        },
        {
            name : "Profile",
            to : "/userProfile"
        }

    ],
};

export default navLinks;